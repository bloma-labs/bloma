{
  "name": "@kolny/anchor-program",
  "version": "0.1.0",
  "private": true,
  "description": "KOLNY colony allocation and settlement program",
  "license": "MIT",
  "scripts": {
    "lint": "prettier --check 'tests/**/*.ts'",
    "lint:fix": "prettier --write 'tests/**/*.ts'"
  },
  "//": "No build/test/deploy scripts here on purpose. `anchor build` is run directly, and the integration tests need a local validator, which is a separate explicit decision. Never add a script that sends a transaction to a cluster.",
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@solana/spl-token": "^0.4.9"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.6",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.9",
    "chai": "^4.5.0",
    "mocha": "^10.8.2",
    "prettier": "^3.3.3",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.6.3"
  }
}
